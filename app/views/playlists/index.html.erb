<h2><%= @current_user.name %>'s Playlists</h2>

<%= link_to 'New Playlist', playlists_new_path %>

<% if notice %>
  <div class="alert alert-success"><%= notice %></div>
<% end %>

<% @playlists.each do |playlist| %>
  <h3> <%= playlist.name %> </h3>
<% end %>